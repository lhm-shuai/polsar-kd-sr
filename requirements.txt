# Core pipeline
numpy>=1.24,<2.0
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0
pyyaml>=6.0
pillow>=10.0
opencv-python>=4.8

# Teacher network (Stage II). The reported runs used torch 2.8+cu128.
torch>=2.0

# Symbolic regression (Stage III). The reported runs used pysr 1.5.9 on
# Julia 1.11.9; the first call installs a matching Julia automatically.
pysr>=1.5.9

# Detector stage. Ultralytics is AGPL-3.0 - see LICENSE.
ultralytics>=8.3
